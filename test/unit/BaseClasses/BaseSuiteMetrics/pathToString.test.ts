import { BaseSuiteMetrics } from "suite-metrics";

suite("[BaseSuiteMetrics] pathToString", function() {

    test("", function() {
        const result = BaseSuiteMetrics.pathToString([]);
    });
});
